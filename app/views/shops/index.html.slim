= form_with url: root_path, method: :get, local: true do
  .ui.icon.input.us-w-100.us-mt-20
    = text_field_tag :q, params[:q], placeholder: '名前で検索', class: 'prompt'
    i.search.icon
- @shops.each do |shop|
  .ui.centered.horizontal.card
    = link_to shop_path(shop), method: :get, class: 'us-p-10' do
      .image
        = image_tag shop.image, class: 'us-img-shop'
      .content
        .header = shop.name
        .description = slice20(shop.description)
- if @shops.count > 1
  .ui.center.aligned.grid.pagination
    == pagy_semantic_nav(@pagy)
