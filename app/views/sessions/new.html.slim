/ ==============================================================================
/ app - views - sessions - new
/ ==============================================================================
- if @error
  ul.ui.red.message
    - unless @mail.blank? && @pass
      li パスワードもしくはメールアドレスが間違っています。
    - if @mail.blank?
      li メールアドレスを入力してください。
    - if @pass
      li パスワードを入力してください。

.ui.container.us-pt-100
  = form_with url: session_path, class: 'form ui form', method: :post, local: true do |f|
    .field
      = label_tag :email, 'メールアドレス'
      = email_field_tag :email, @email, placeholder: 'email@example.com'
    .field
      = label_tag :password, 'パスワード'
      = password_field_tag :password
    = submit_tag "ログイン", class: 'ui button primary us-w-100'
  .ui.horizontal.divider または
  .us-center
    = link_to new_user_path  do
      .ui.teal.labeled.icon.button.us-fw-100.us-w-60
        新規登録
        i.add.icon
